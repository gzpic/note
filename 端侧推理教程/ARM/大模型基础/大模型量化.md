---

---
大模型中95%的参数集中在线性层，包括前馈网络的两层全连接和注意力机制的Q/K/V/O投影。这些矩阵乘法不仅占据了绝大部分显存，还贡献了65-85%的计算量。

INT8矩阵乘法在NVIDIA GPU的Tensor Core上有专门的硬件支持，峰值算力远高于FP16。

量化方案的本质不是“bit 数”，而是“谁来承担 outlier（这个可删除）

可通过软件方式实现w4和w3怎么实现呢，重点，以及他的优缺点

tensorcore喜欢连续的数据流llm.int8打破了连续性，kernel launch次数也增加了，显存搬运碎片了
