 ```
1.向量化的读取(通用的)(int4,float4,half2)

  

half4* out = reinterpret_cast<half4 *>&((*in)[0])  // 错了呢

half4 *out = reinterpret_cast<half4 *>(&(in))[0];

  

2.gemm

暂时还没有

  

3.rmsnorm

  

公式主要是gamma/beta/epsilong//和layernorm的区别（差一个均值）

  
  
  
  

beta* x/(根号(x*x/len + epsilon))

rsqrtf(x)/(1/(根号x))

gamma一个向量长度=hidden_state

epsilon全局就一个呢

  

4.reducesum

block_reduce,warpreduce

  

template<int width = WARP_SIZE>

static __device__ __forceinline__ int warp_reduce_sum(int x) {

#pragma unroll

    for (int offset = width/2; offset > 0; offset >>= 1) {

        x += __shfl_xor_sync(0xffffffff, x, offset, width);

    }

    return x;

}

  

static __device__ __forceinline__ int block_reduce_sum(int x) {

    __shared__ float s_sum[32];

    const int warp_id = threadIdx.x / WARP_SIZE;

    const int lane_id = threadIdx.x % WARP_SIZE;

    float tmp = 0.0f; // partial sum for thread in warp

    if (lane_id == 0) {

        s_sum[warp_id] = tmp;

    }

    __syncthreads();

    tmp = s_sum[lane_id];

    tmp = warp_reduce_sum(tmp);

    return tmp;

}

  

5.prefix_sum

Kogge–Stone：
for (int offset = 1; offset < blockDim.x; offset <<= 1) {
    if (g_tid >= offset) {
        float val = input[g_tid - offset]; // else =0
        __threadsync();
        block_prefix[tid] += val;
        __threadsync(); // __syncthreads();
    }
}


__syncthreads();

6.topk

  

7.transpose

__global__ void transposeNaiveRow(float* out, float* in, const int nx, const int ny)
{
    unsigned int ix = blockDim.x * blockIdx.x + threadIdx.x;
    unsigned int iy = blockDim.y * blockIdx.y + threadIdx.y;

    if (ix < nx && iy < ny)
    {
        out[ix * ny + iy] = in[iy * nx + ix];
    }
}
  
  __global__ void transposeSmem(float* out, float* in, int nx, int ny)
{
    __shared__ float tile[BDIMY][BDIMX];

    unsigned int ix = blockIdx.x * blockDim.x + threadIdx.x;
    unsigned int iy = blockIdx.y * blockDim.y + threadIdx.y;
    unsigned int ti = iy * nx + ix;

    unsigned int bidx = threadIdx.y * blockDim.x + threadIdx.x;
    unsigned int irow = bidx / blockDim.y;
    unsigned int icol = bidx % blockDim.y;

    unsigned int ox = blockIdx.y * blockDim.y + icol;
    unsigned int oy = blockIdx.x * blockDim.x + irow;
    unsigned int to = oy * ny + ox;

    if (ix < nx && iy < ny)
    {
        tile[threadIdx.y][threadIdx.x] = in[ti];
        __syncthreads();

        if (ox < ny && oy < nx)
        {
            out[to] = tile[icol][irow];
        }
    }
}

  __global__ void transposeSmemUnrollPad(float* out, float* in, const int nx, const int ny)
{
    // 1D shared memory，带 padding，避免 bank conflict
    __shared__ float tile[BDIMY * (BDIMX * 2 + IPAD)];

    // 原矩阵中的全局坐标
    unsigned int ix = 2 * blockIdx.x * blockDim.x + threadIdx.x;
    unsigned int iy = blockIdx.y * blockDim.y + threadIdx.y;

    // 原矩阵中的线性索引
    unsigned int ti = iy * nx + ix;

    // 当前线程在线性 block 内的编号
    unsigned int bidx = threadIdx.y * blockDim.x + threadIdx.x;

    // 转置后，线程对应到 shared memory 中的新行列
    unsigned int irow = bidx / blockDim.y;
    unsigned int icol = bidx % blockDim.y;

    // 转置矩阵中的全局坐标
    unsigned int ix2 = blockIdx.y * blockDim.y + icol;
    unsigned int iy2 = 2 * blockIdx.x * blockDim.x + irow;

    // 转置矩阵中的线性索引
    unsigned int to = iy2 * ny + ix2;

    if (ix + blockDim.x < nx && iy < ny)
    {
        // 从全局内存加载两段数据到 shared memory
        unsigned int row_idx = threadIdx.y * (blockDim.x * 2 + IPAD) + threadIdx.x;
        tile[row_idx] = in[ti];
        tile[row_idx + BDIMX] = in[ti + BDIMX];

        __syncthreads();

        // 从 shared memory 按列读出，写回全局内存，实现转置
        unsigned int col_idx = icol * (blockDim.x * 2 + IPAD) + irow;
        out[to] = tile[col_idx];
        out[to + ny * BDIMX] = tile[col_idx + BDIMX];
    }
}

8.topp

  
  

9.online softmax,softmax
softmax:
float y = expf(x); // x 是 float  
double y = exp(x); // x 是 doublena


  

10. flashattention


  

11. mha/gqa/mqa

  

12. cache read/ write /kvcache

  

13. 反量化/量化

14.
```
公式有：
soft'max:
![[Pasted image 20260413210415.png]]

e

哪些算子需要中途转成其他类型例如float再转回来![[Pasted image 20260413211411.png]]的 

![[Pasted image 20260413211433.png]]
![[Pasted image 20260413211756.png]]