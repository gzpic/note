---

---
1. 

./gemm_double_buffer

nvcc -arch=sm_87 main.cu gemm.cu -o gemm_double_buffer

sudo /usr/local/cuda-12.6/bin/ncu -o ncu_output ./gemm_double_buffer

sudo /usr/local/cuda-12.6/bin/ncu \
--kernel-name "hgemm_t_8x8_sliced_k_f16x4_pack_kernel" \
--launch-count 1 \
--section LaunchStats \
--section Occupancy \
--section MemoryWorkloadAnalysis \
./gemm_double_buffer

sudo /usr/local/cuda-12.6/bin/ncu \
--kernel-name "hgemm_t8x4_bm64_bn64_bk32_f16_acc_f32_opt" \
--launch-count 1 \
--section LaunchStats \
--section Occupancy \
--section MemoryWorkloadAnalysis \
./gemm_double_buffer

ncu --stats achieved_occupancy,theoretical_warps_per_sm,active_warps_per_sm,sm__throughput,inst_fp16,inst_tensor,eligible_warps_per_cycle,dram__throughput,l2_throughput,gld_efficiency --export basic.txt--launch <hgemm_t_8x8_sliced_k_f16x4_pack_kernel> --target-processes all ./your_program


sudo /usr/local/cuda-12.6/bin/ncu --target-processes all \
--kernel-name-base function \
--kernel-name "hgemm_t_8x8_sliced_k_f16x4_pack_kernel*" \
--metrics achieved_occupancy,theoretical_warps_per_sm,active_warps_per_sm,sm__throughput,inst_fp16,inst_tensor,eligible_warps_per_cycle,dram__throughput,l2_throughput,gld_efficiency \
--page raw --csv \
--log-file basic_metrics.csv --force-overwrite \
./gemm_basic

sudo /usr/local/cuda-12.6/bin/ncu --target-processes all \
--kernel-name-base function \
--kernel-name "hgemm_t_8x8_sliced_k_f16x4_pack_kernel*" \
--metrics achieved_occupancy,theoretical_warps_per_sm,active_warps_per_sm,sm__throughput,inst_fp16,inst_tensor,eligible_warps_per_cycle,dram__throughput,l2_throughput,gld_efficiency \
./gemm_basic

sudo /usr/local/cuda-12.6/bin/ncu --target-processes all \
--kernel-name-base function \
--kernel-name "hgemm_t_8x8_sliced_k_f16x4_pack_kernel" \
--set full\
--export gemm_basic.ncu-rep --force-overwrite \
./gemm_basic
